<Window x:Class="WPFRouteBeheerUI.AddPointDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFRouteBeheerUI"
        mc:Ignorable="d"
        Title="Add Point" Height="400" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="#1F2129">

    <Window.Resources>
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#2A2D3E"/>
            <Setter Property="Foreground" Value="#F5F7FB"/>
            <Setter Property="BorderBrush" Value="#252836"/>
        </Style>

        <Style TargetType="Button" x:Key="ActionButton">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Background" Value="#6C5CE7"/>
            <Setter Property="Foreground" Value="#F5F7FB"/>
            <Setter Property="BorderBrush" Value="#252836"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#F5F7FB"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#252836" BorderThickness="1" Background="#1F2129" CornerRadius="4" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!--De custom Top Bar-->
            <Grid Background="#252836" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Grid.Row="0">
                <TextBlock Text="Add Point" Foreground="#F5F7FB" VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="SemiBold"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource WindowControlButton}" Content="&#xE8BB;" Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>

            <ListBox Grid.Row="1" Name="ListBoxPoints" Margin="0,10,0,10" Background="#2A2D3E" Foreground="#F5F7FB">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Left" >
                            <TextBlock Text="ID:" Foreground="#F5F7FB" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Id}" Width="40" TextAlignment="Center" Foreground="#F5F7FB"/>

                            <TextBlock Text="X:" Foreground="#F5F7FB" Margin="15,0,5,0"/>
                            <TextBlock Text="{Binding X}" Width="60" TextAlignment="Center" Foreground="#F5F7FB"/>

                            <TextBlock Text="Y:" Foreground="#F5F7FB" Margin="15,0,5,0"/>
                            <TextBlock Text="{Binding Y}" Width="60" TextAlignment="Center" Foreground="#F5F7FB"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Button Grid.Row="2" Content="Select" Style="{StaticResource ActionButton}" HorizontalAlignment="Stretch" Click="SelectButton_Click"/>
        </Grid>
    </Border>
</Window>
